Compilado con SASS live compiler

He usado un "array" de colores, y variables. 

Con el map se asigna los colores a la variable base-color dependiendo de la iteración del bucle.  
Con el bucle for se itera los contenedores correspondiendo al numero de colores base, 
y en el otro for se itera por las cajas que hay y se acalrando y oscurenciendo el color junto con el texto.  